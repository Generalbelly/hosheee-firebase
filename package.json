{
  "name": "hosheee",
  "scripts": {
    "deploy:dev": "firebase use hosheee-dev && firebase deploy --only firestore:rules,firestore:indexes,storage",
    "deploy:prod": "firebase use hosheee && firebase deploy --only firestore:rules,firestore:indexes,storage",
    "deploy-hosting:dev": "firebase use hosheee-dev && firebase deploy --only hosting",
    "deploy-hosting:prod": "firebase use hosheee && firebase deploy --only hosting"
  },
  "dependencies": {},
  "devDependencies": {},
  "private": true
}
